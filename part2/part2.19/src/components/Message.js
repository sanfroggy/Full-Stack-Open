//Defining a message component to display a given message.
const Message = ({ message }) => {
    return (
        <div>
            <p>{message}</p>
        </div>
    )
}

export default Message
