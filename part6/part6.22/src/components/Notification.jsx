const Notification = () => {

    /*Defining an inline-style for the message
    to be displayed. */
    const style = {
        border: 'solid',
        padding: 10,
        borderWidth: 1,
        marginBottom: 5
    }
  
    if (true) return null

    return (
        <div style={style}>
      
        </div>
    )
}

export default Notification
